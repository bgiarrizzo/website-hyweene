<feed xmlns="http://www.w3.org/2005/Atom" xmlns:thr="http://purl.org/syndication/thread/1.0" xml:lang="fr">
  <id>{{ site.url }}/liens/</id>
  <title>{{ site.short_url }} - Liens</title>
  <subtitle>{{ site.description }}</subtitle>
  <link rel="self" type="application/atom+xml" href="{{ site.url }}/liens/feed.xml"></link>
  <link href="{{ site.url }}/liens/"></link>

  {% set last_link = links|last %}
  <updated>{{ last_link.publish_date.rfc3339 }}</updated>

  <author>
    <name>{{ site.author }}</name>
    <uri>{{ site.url }}</uri>
  </author>

  {% for link in links | sort(attribute='publish_date.original', reverse=True) %}
    <entry>
      <id>{{ site.url }}/liens/{{ link.path }}/</id>
      <title>{{ link.title }}</title>
      <link rel="alternate" type="text/html" href="{{ site.url }}/liens/{{ link.path }}/"></link>
      <published>{{ link.publish_date.rfc3339 }}</published>
      {% if link.update_date and link.update_date != link.publish_date %}
        <updated>{{ link.update_date.rfc3339 }}</updated>
      {% else %}
        <updated>{{ link.publish_date.rfc3339 }}</updated>
      {% endif %}

      {% if link.summary %}
        <summary>
          {{ link.summary }}
        </summary>
      {% endif %}

      <content type="html">
        {{ link.body | e }}
      </content>

    </entry>
  {% endfor %}
</feed>
